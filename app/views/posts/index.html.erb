Welcome <%=current_user.name%>
  <br>
  <%= form_for @post do |form| %>
    <%= form.label :message %>
    <%= form.text_area :message, placeholder: "What's on your mind?" %>
    <%= form.hidden_field :wall_id, value: 0 %>

    <%= form.submit "Submit" %>
  <% end %>


<ul>
<%if @posts%>
  <% @posts.each do |post| %>
    <p> <%=post.created_at.strftime("%d %b %Y at %k:%M") %><br><%= simple_format(post.message) %><%= post.user.name %></p>
    <%= link_to "Edit Post", edit_post_path(post) %>
    <%= link_to "Delete Post", post_path(post), :method => :delete %>
    <%= form_with(model: [ @posts, @posts.comments.build ], local: true) do |form| %>
    <p> <%= form.label :commenter %><br>
        <%= form.text_field :commenter %>
        </p>
        <p>
        <%= form.label :body %><br>
        <%= form.text_area :body %>
        </p>
        <p>
        <%= form.submit %>
        </p>
        <% end %>
      <% end %>
    <%end%>
